[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "membridge"
version = "0.1.3"

[tool.maturin]
include = [
    { path = "membridge.pyi", format = "wheel" }
]